/**
 * Centralized Course, Learning, and Certification Business Constants
 */

// Minimum percentage of video duration (0.0 to 1.0) required to consider a video watched
export const VIDEO_COMPLETION_THRESHOLD = 0.9;

// Default minimum passing score (0 to 100) required to unlock course certificate
export const DEFAULT_MIN_CERTIFICATE_SCORE = 70;
